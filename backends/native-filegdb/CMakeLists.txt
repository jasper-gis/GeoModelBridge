cmake_minimum_required(VERSION 3.21)
project(GeoModelBridgeNative VERSION 0.1.5 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(FILEGDB_API_ROOT "" CACHE PATH "Extracted official FileGDB API 1.5.5 Windows VS2022 SDK")
option(GMB_INSTALL_FILEGDB_RUNTIME "Install the official release FileGDBAPI.dll with its license and README" OFF)
if(NOT WIN32 OR NOT MSVC)
  message(FATAL_ERROR "The native FileGDB Windows C++ SDK uses the Microsoft STL ABI. Configure this separate backend with an x64 MSVC toolchain.")
endif()
if(CMAKE_CONFIGURATION_TYPES)
  set(CMAKE_CONFIGURATION_TYPES "Release;RelWithDebInfo;MinSizeRel" CACHE STRING "Configurations compatible with the release FileGDB SDK" FORCE)
else()
  if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Release CACHE STRING "Native writer build configuration" FORCE)
  endif()
  if(NOT CMAKE_BUILD_TYPE MATCHES "^(Release|RelWithDebInfo|MinSizeRel)$")
    message(FATAL_ERROR "Only Release, RelWithDebInfo and MinSizeRel are supported. Debug /MDd and iterator-debug STL ABI are incompatible with the release FileGDBAPI.lib.")
  endif()
endif()
if(NOT EXISTS "${FILEGDB_API_ROOT}/include/FileGDBAPI.h" OR NOT EXISTS "${FILEGDB_API_ROOT}/lib64/FileGDBAPI.lib")
  message(FATAL_ERROR "Set FILEGDB_API_ROOT to your extracted official Windows x64 FileGDB API SDK.")
endif()
get_filename_component(GMB_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../.." ABSOLUTE)
add_executable(GeoModelBridge.NativeWriter main.cpp images.cpp "${GMB_ROOT}/src/core.cpp")
set_property(TARGET GeoModelBridge.NativeWriter PROPERTY MSVC_RUNTIME_LIBRARY MultiThreadedDLL)
target_include_directories(GeoModelBridge.NativeWriter PRIVATE "${GMB_ROOT}/include" "${GMB_ROOT}/third_party")
target_include_directories(GeoModelBridge.NativeWriter SYSTEM PRIVATE "${FILEGDB_API_ROOT}/include")
target_compile_definitions(GeoModelBridge.NativeWriter PRIVATE NOMINMAX UNICODE _UNICODE _ITERATOR_DEBUG_LEVEL=0)
target_compile_options(GeoModelBridge.NativeWriter PRIVATE /W4 /permissive- /utf-8)
target_link_libraries(GeoModelBridge.NativeWriter PRIVATE "${FILEGDB_API_ROOT}/lib64/FileGDBAPI.lib" windowscodecs ole32)
install(TARGETS GeoModelBridge.NativeWriter RUNTIME DESTINATION bin/native-filegdb)
if(GMB_INSTALL_FILEGDB_RUNTIME)
  install(FILES "${FILEGDB_API_ROOT}/bin64/FileGDBAPI.dll" DESTINATION bin/native-filegdb)
  install(FILES "${FILEGDB_API_ROOT}/license/Apache License.pdf" "${FILEGDB_API_ROOT}/license/userestrictions.txt" "${FILEGDB_API_ROOT}/README-windows_VS2022.txt" DESTINATION licenses/filegdb-api)
  install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/sdk-sources.json" DESTINATION licenses/filegdb-api)
endif()
# With runtime installation disabled, add FILEGDB_API_ROOT/bin64 to PATH.
