# Changelog

## V0.1.6 — 2026-09-21

- 增加 Ubuntu 24.04 x86_64 原生 FileGDB 转换，Windows/Linux 共用 C++17 核心、Scene Bundle、材质编解码和写入/回读逻辑，继续直接维护 master。
- 分离平台路径、UTF 编码、不覆盖提交和图片解码；Linux 使用官方 FileGDB SDK、libpng/libjpeg，PNG 保留直通 RGBA，JPEG 验证后保留原压缩字节。
- 增加统一 Python/CMake 构建入口、固定 Linux SDK 下载与散列校验、可搬迁运行库布局、tar.gz 打包及 Ubuntu 原生 CI。WPF GUI 仍仅支持 Windows。
- 补充跨平台图片、Unicode、Linux PATH/符号链接/大小写测试，完善 README 部署与维护说明。实际结果见 [V0.1.6 验证记录](docs/validation-v0.1.6.md)，图形验收保持独立。

## V0.1.5 — 2026-09-21

- 仅保留原生 FileGDB 后端：移除 Pro 适配器、托管 DLL 启动、授权探测及相关构建/验证脚本；CLI 拒绝旧后端，GUI 固定原生转换。
- 构建、样例生成与发布检查不再要求 Pro 或其独立回读工具；发布使用明确目录和文件清单，拒绝混入旧后端，保留旧安装。
- 更新当前部署说明、版本声明与回归；历史报告和显示验收边界保持原始来源。具体执行结果见 [V0.1.5 验证记录](docs/validation-v0.1.5.md)。
- 仓库使用 master 直接提交，不创建其他分支。

## V0.1.4 — 2026-09-21

- Scene Bundle 的网格与角点改为逐项写出，避免在内存中同时构造整份 JSON 对象和文本。真实模型准备阶段峰值工作集从约 282 MB 降至约 60 MB，生成的 `scene.json` 与优化前逐字节相同；该测量不包含数据库写入后端。
- 复用 JPEG 封装修复时已经计算的图片散列，避免同一图片重复计算；几何、材质、贴图和诊断内容保持不变。
- GUI 对两路进程日志按固定大小读取，限制预览长度与通知数量，明确说明折叠条数；大量普通输出不会淹没末尾错误。
- 中文报告在缺少必要回读验证信息时不再显示成功，列出需要核对的字段；转换服务的严格结果核验保持生效。
- CTest 5/5、GUI 136/136（含实际原生转换）通过。保留 V0.1.3，新增独立 `releases/V0.1.4` 目录，不生成 ZIP。显示验收与既有坐标量化差异未在本次宣称解决。

## V0.1.3 — 2026-09-20

- 修复标准材质中性反射率、置换颜色和向量置换颜色被误报的问题；区分空动画容器与真实动画曲线，空容器不再阻断静态转换。
- 增加 `strict` / `gis-static` 转换策略。命令行保持默认严格检查；GUI 默认明确展示 GIS 静态兼容：使用文件保存的静态姿态，省略环境光、高光和反射通道，删除有限坐标下严格零面积的三角形，每项处理均写入报告。
- 保留缺少漫反射贴图、无效 UV、非有限几何、活动位移、PBR、自发光及未知渲染内容的拒绝检查；极小但非零或数值不安全的面不能冒充零面积清理成功。
- 对缺少 JFIF、明确为 8 位三分量 Adobe YCbCr 基线 且 EXIF 方向缺省或为 1 的 JPEG，GIS 策略补充标准 18 字节 JFIF APP0 并记录原/新 SHA-256；原压缩数据不重新编码，源图片不改动。严格策略要求先处理封装，颜色空间或方向不明确时拒绝自动修复。
- GUI 增加“换个新名称”、兼容处理完成提示及“中文摘要 / 原始 JSON”报告页签；重复诊断按原因归并，展示对象示例、建议和有效的数量统计，失败后可保留原报告再重试。
- 成功报告要求转换策略与请求一致；退出码为 0 但报告失败时仍返回失败，并展示中文原因，不能仅凭进程退出判断 GDB 成功。
- CTest 5/5、转换策略合成回归 78/78、GUI 逻辑与真实原生转换测试 125/125 通过。新增 JPEG 封装、颜色/方向/像素比例前提、截断或缺失扫描数据的边界回归；修正 Maya 原始二进制样例预期，验证中性默认字段和空动画容器在两种策略下生成相同几何与材质。证据位于 `docs/evidence/V0.1.3`。
- 用户模型的 3 张 JPEG 完成封装修复后，原生与 Pro 后端各写入并自回读 56 个要素。原生成果通过独立 Pro 按中间包核验 69,936 个三角形及 12 张贴图。两 writer 坐标分辨率不同，跨 writer 的精确 signature 不同；未宣称逐位一致或图形外观验收通过。
- 继续以工程目录交付，不生成 ZIP；历史成果、图形验收待办和各版本原始证据保留。

## V0.1.2 — 2026-09-20

- 新增 Windows x64 中文桌面程序 `geomodelbridgeGUI.exe`，提供 FBX/输出路径选择、原生或 Pro 后端、WKID/XYZ 原点、要素类和附加贴图目录设置。
- 增加环境检查、显式加载演示参数、实时日志、打开输出目录和查看报告操作；复用现有 CLI 与后端的严格转换及重开核验流程。
- GUI 以自包含单文件形式发布；转换仍需同目录 CLI 与后端组件，选择 Pro 时仍需本机安装且许可可用的 ArcGIS Pro。
- 构建脚本增加 `-WithGui`；版本检查覆盖 GUI 项目与运行时代码，内核及两个后端版本统一为 0.1.2。
- CTest 4/4、GUI 共享逻辑测试 84/84、原生 29 项/Pro 19 项集成回归通过；实际窗口完成演示转换、查看报告、防覆盖及切换模型后的坐标清空检查。细项见 [V0.1.2 验证记录](docs/validation-v0.1.2.md)。
- 按用户要求改为工程目录交付，删除现有 ZIP 与对应校验文件，本版不生成 ZIP；保留 V0.1.0/V0.1.1 原始样例及验证证据。
- GUI 功能不等同于目标场景视觉验收。V0.1.1 记录中的红青双影、完整色彩/Alpha/六面接缝待验结论继续保留。

## V0.1.1 — 2026-09-20

- 新增独立 MSVC x64 / FileGDB API 1.5.5 后端，目标转换过程不调用 ArcGIS Pro，仍沿用同一 Scene Bundle 和严格输入校验。
- 为两个后端增加明确的目标纹理坐标映射：所有有 UV 的 patch 写入 `U=sourceU, V=1-sourceV`，修复首版图形调查中发现的上下翻转；无纹理材质采用相同 UV 语义，图片行序和源 UV 保持不变。
- CLI 可选择 `native-filegdb`，使用独立程序路径与依赖探测，不再固定返回原生后端未实现。
- CLI 强制成功报告的后端、版本、要素类与请求一致，拒绝将外部旧 V0.1.0 writer 的成果报告为本版成功；打包脚本验证 Pro 实际二进制版本。
- 新增 ArcGIS Pro 图形验收工程脚本，设置本地三维场景、绝对高程、固定视角和多视角布局。
- 增加源码版本一致性检查，保留 V0.1.0 历史 ZIP、样例和验证证据。
- 提供可选构建参数，随原生程序附带官方 release FileGDBAPI.dll、原始 Apache-2.0 许可与来源散列；完整 SDK、Microsoft 开发组件及 ArcGIS Pro 程序集不打包。
- 完整样例脚本支持 `--backend native-filegdb`；4 组 CTest、双后端各 14 例转换与独立复制、Pro 19 项/原生 29 项失败处理回归全部通过，14 例原生库均由独立 Pro 精确读取比对通过。
- PNG/JPEG 方向参考图的实际目标显示通过，原生与 Pro 对应导出一致。红青双影尚影响完整色彩、Alpha 及六面/接缝图形验收；未将未知显示设置写成已确认原因。证据见 [V0.1.1 验证记录](docs/validation-v0.1.1.md)。

## V0.1.0 — 2026-09-20

- 从零建立 GeoModelBridge C++17/CMake 工程及独立命令行。
- 固定 ufbx 0.23.0，读取静态 FBX、实例/节点变换、角点 UV/法线、基础材质和纹理。
- 提供 strict validation、SHA-256 资源打包、机器可读转换报告和拒绝覆盖机制。
- 增加五组 writer 优先的合成测试网格和自造 FBX 回归样本。
- 增加独立 ArcGIS Pro CoreHost 后端，创建实际 FileGDB Multipatch，关闭/重开后核对几何、材质、UV、法线及纹理。
- 识别并验证本机 Pro 3.6.2 写 GDB 的法线分量 1/128 量化规则，单独报告源值与目标方向误差；包含斜面和半步边界回归。
- 原生 FileGDB textured writer 明确标记为未就绪；未实现自动烘焙、坐标重投影和批量优化。
