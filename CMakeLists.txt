cmake_minimum_required(VERSION 3.21)
project(GeoModelBridge VERSION 0.3.0 LANGUAGES C CXX)

option(GMB_BUILD_TESTS "Build correctness tests" ON)
option(GMB_BUILD_NATIVE "Build the complete CLI with native FileGDB writer (requires the platform SDK)" ON)
option(GMB_STATIC_RUNTIME "Link MinGW runtime statically for a portable CLI" ON)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# One root configure owns both executables, the SDK and their runtime layout.
if(GMB_BUILD_NATIVE)
  if(DEFINED ENV{FILEGDB_API_ROOT} AND NOT "$ENV{FILEGDB_API_ROOT}" STREQUAL "")
    set(GMB_DEFAULT_SDK "$ENV{FILEGDB_API_ROOT}")
  else()
    set(GMB_DEFAULT_SDK "${CMAKE_CURRENT_SOURCE_DIR}/build/filegdb-sdk")
  endif()
  set(FILEGDB_API_ROOT "${GMB_DEFAULT_SDK}" CACHE PATH "Extracted official FileGDB API 1.5.5 SDK for this platform")
  get_filename_component(FILEGDB_API_ROOT "${FILEGDB_API_ROOT}" ABSOLUTE BASE_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
  option(GMB_INSTALL_FILEGDB_RUNTIME "Copy and install official FileGDB shared libraries and original notices" ON)
  if(WIN32 AND NOT MSVC)
    message(FATAL_ERROR "The Windows FileGDB SDK requires the Microsoft STL ABI. Use an x64 MSVC toolchain.")
  elseif(NOT WIN32 AND NOT CMAKE_SYSTEM_NAME STREQUAL "Linux")
    message(FATAL_ERROR "The native writer supports Windows x64 and Linux x86_64.")
  endif()
  if(NOT CMAKE_SIZEOF_VOID_P EQUAL 8 OR (NOT WIN32 AND NOT CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|amd64|AMD64)$"))
    message(FATAL_ERROR "The pinned FileGDB SDK requires x86_64.")
  endif()
  if(CMAKE_CONFIGURATION_TYPES)
    set(CMAKE_CONFIGURATION_TYPES "Release;RelWithDebInfo;MinSizeRel" CACHE STRING "Configurations compatible with the release FileGDB SDK" FORCE)
  else()
    if(NOT CMAKE_BUILD_TYPE)
      set(CMAKE_BUILD_TYPE Release CACHE STRING "Native writer build configuration" FORCE)
    endif()
    if(NOT CMAKE_BUILD_TYPE MATCHES "^(Release|RelWithDebInfo|MinSizeRel)$")
      message(FATAL_ERROR "Only Release, RelWithDebInfo and MinSizeRel are supported. Debug /MDd and iterator-debug STL ABI are incompatible with the release FileGDBAPI.lib.")
    endif()
  endif()
  if(NOT EXISTS "${FILEGDB_API_ROOT}/include/FileGDBAPI.h")
    message(FATAL_ERROR "FileGDB SDK not found at ${FILEGDB_API_ROOT}. From the repository root run: python scripts/fetch_filegdb_sdk.py --output build/filegdb-sdk. Or set -DFILEGDB_API_ROOT=<SDK> / FILEGDB_API_ROOT. For inspect/prepare only, explicitly use -DGMB_BUILD_NATIVE=OFF.")
  endif()
endif()

set(GMB_RUNTIME_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/bin")
if(CMAKE_CONFIGURATION_TYPES)
  string(APPEND GMB_RUNTIME_DIRECTORY "/$<CONFIG>")
endif()
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${GMB_RUNTIME_DIRECTORY}")

add_library(gmb_ufbx STATIC third_party/ufbx/ufbx.c)
target_include_directories(gmb_ufbx PUBLIC third_party/ufbx)
if(UNIX)
  target_link_libraries(gmb_ufbx PUBLIC m)
endif()
add_library(GeoModelBridge.Core STATIC src/core.cpp src/bundle.cpp src/fbx_reader.cpp src/fixtures.cpp)
if(WIN32)
  target_sources(GeoModelBridge.Core PRIVATE src/platform_files_windows.cpp)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  target_sources(GeoModelBridge.Core PRIVATE src/platform_files_linux.cpp)
else()
  message(FATAL_ERROR "Safe output commits are supported on Windows and Linux.")
endif()
add_library(gmb::core ALIAS GeoModelBridge.Core)
target_include_directories(GeoModelBridge.Core PUBLIC include PRIVATE third_party)
target_link_libraries(GeoModelBridge.Core PRIVATE gmb_ufbx)
add_executable(geomodelbridge src/main.cpp)
target_include_directories(geomodelbridge PRIVATE third_party)
target_link_libraries(geomodelbridge PRIVATE gmb::core)
if(WIN32)
  target_link_libraries(geomodelbridge PRIVATE shell32)
endif()
foreach(target GeoModelBridge.Core geomodelbridge)
  if(MSVC)
    target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8)
  else()
    target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic)
  endif()
endforeach()
if(MINGW AND GMB_STATIC_RUNTIME)
  target_link_options(geomodelbridge PRIVATE -static -static-libgcc -static-libstdc++)
endif()
include(CTest)
if(BUILD_TESTING AND GMB_BUILD_TESTS)
  add_executable(gmb_core_tests tests/core_tests.cpp)
  target_link_libraries(gmb_core_tests PRIVATE gmb::core)
  target_include_directories(gmb_core_tests PRIVATE third_party)
  if(MINGW AND GMB_STATIC_RUNTIME)
    target_link_options(gmb_core_tests PRIVATE -static -static-libgcc -static-libstdc++)
  endif()
  add_test(NAME core COMMAND gmb_core_tests)
  find_package(Threads REQUIRED)
  add_executable(gmb_output_tests tests/output_tests.cpp)
  target_link_libraries(gmb_output_tests PRIVATE gmb::core Threads::Threads)
  target_include_directories(gmb_output_tests PRIVATE third_party)
  if(MSVC)
    target_compile_options(gmb_output_tests PRIVATE /W4 /permissive- /utf-8)
  elseif(MINGW AND GMB_STATIC_RUNTIME)
    target_link_options(gmb_output_tests PRIVATE -static -static-libgcc -static-libstdc++)
  endif()
  add_test(NAME output_safety COMMAND gmb_output_tests)
  find_package(Python3 COMPONENTS Interpreter QUIET)
  if(Python3_Interpreter_FOUND AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tests/cli_test.py")
    add_executable(gmb_failing_writer tests/failing_writer.cpp)
    add_executable(gmb_report_writer tests/report_writer.cpp)
    target_link_libraries(gmb_report_writer PRIVATE gmb::core)
    target_include_directories(gmb_report_writer PRIVATE third_party)
    if(MINGW AND GMB_STATIC_RUNTIME)
      target_link_options(gmb_failing_writer PRIVATE -static -static-libgcc -static-libstdc++)
      target_link_options(gmb_report_writer PRIVATE -static -static-libgcc -static-libstdc++)
    endif()
    add_test(NAME cli COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/cli_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures" "$<TARGET_FILE:gmb_failing_writer>" "$<TARGET_FILE:gmb_report_writer>")
    add_test(NAME reader_integration COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/reader_integration_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME binary_fbx COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/binary_fbx_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME obj_integration COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/obj_integration_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME profile_integration COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/profile_integration_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME missing_textures COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/missing_texture_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME normal_repair COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/normal_repair_test.py" "$<TARGET_FILE:geomodelbridge>" "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures")
    add_test(NAME python_client COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/python_client_test.py")
  endif()
endif()
install(TARGETS geomodelbridge RUNTIME DESTINATION bin)
install(FILES tests/fixtures/textured_quad.fbx tests/fixtures/textured_quad.obj tests/fixtures/textured_quad.mtl tests/fixtures/checker.png DESTINATION bin/demo)
install(DIRECTORY python/geomodelbridge DESTINATION python FILES_MATCHING PATTERN "*.py" PATTERN "__pycache__" EXCLUDE)
install(FILES python/examples/convert_fbx.py DESTINATION python/examples)
if(GMB_BUILD_NATIVE)
  add_executable(GeoModelBridge.NativeWriter backends/native-filegdb/main.cpp backends/native-filegdb/images.cpp backends/native-filegdb/images_jpeg.cpp backends/native-filegdb/images_png.cpp "${CMAKE_CURRENT_SOURCE_DIR}/src/core.cpp")
  target_include_directories(GeoModelBridge.NativeWriter PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/include" "${CMAKE_CURRENT_SOURCE_DIR}/third_party")
  target_include_directories(GeoModelBridge.NativeWriter SYSTEM PRIVATE "${FILEGDB_API_ROOT}/include")
  if(WIN32)
    # Only the decoder is built, from unchanged, hash-pinned upstream sources.
    # Keep its configuration scoped and all project targets in this root build.
    function(gmb_add_windows_jpeg)
      set(GMB_JPEG_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/third_party/libjpeg-turbo")
      set(GMB_JPEG_BINARY "${CMAKE_CURRENT_BINARY_DIR}/jpeg-config")
      set(JPEG_LIB_VERSION 62)
      set(VERSION 3.1.4.1)
      set(LIBJPEG_TURBO_VERSION_NUMBER 3001004)
      set(BUILD "GeoModelBridge")
      set(CMAKE_PROJECT_NAME "libjpeg-turbo")
      set(COPYRIGHT_YEAR 2026)
      set(SIZE_T 8)
      set(INLINE "__inline")
      set(THREAD_LOCAL "__declspec(thread)")
      set(HIDDEN "")
      set(HAVE_INTRIN_H 1)
      set(D_ARITH_CODING_SUPPORTED 1)
      set(C_ARITH_CODING_SUPPORTED 0)
      set(WITH_SIMD 0)
      foreach(header jconfig.h jconfigint.h jversion.h)
        configure_file("${GMB_JPEG_SOURCE}/src/${header}.in" "${GMB_JPEG_BINARY}/${header}" @ONLY)
      endforeach()
      set(GMB_JPEG_DECODER_SOURCES
        src/jcomapi.c src/jdapimin.c src/wrapper/jdapistd-8.c
        src/wrapper/jdapistd-12.c src/wrapper/jdapistd-16.c src/jdatasrc.c
        src/wrapper/jdcoefct-8.c src/wrapper/jdcoefct-12.c src/wrapper/jdcolor-8.c
        src/wrapper/jdcolor-12.c src/wrapper/jdcolor-16.c src/wrapper/jddctmgr-8.c
        src/wrapper/jddctmgr-12.c src/wrapper/jddiffct-8.c src/wrapper/jddiffct-12.c
        src/wrapper/jddiffct-16.c src/jdhuff.c src/jdicc.c
        src/jdinput.c src/jdlhuff.c src/wrapper/jdlossls-8.c
        src/wrapper/jdlossls-12.c src/wrapper/jdlossls-16.c src/wrapper/jdmainct-8.c
        src/wrapper/jdmainct-12.c src/wrapper/jdmainct-16.c src/jdmarker.c
        src/jdmaster.c src/wrapper/jdmerge-8.c src/wrapper/jdmerge-12.c
        src/jdphuff.c src/wrapper/jdpostct-8.c src/wrapper/jdpostct-12.c
        src/wrapper/jdpostct-16.c src/wrapper/jdsample-8.c src/wrapper/jdsample-12.c
        src/wrapper/jdsample-16.c src/jdtrans.c src/jerror.c
        src/wrapper/jidctflt-8.c src/wrapper/jidctflt-12.c src/wrapper/jidctfst-8.c
        src/wrapper/jidctfst-12.c src/wrapper/jidctint-8.c src/wrapper/jidctint-12.c
        src/wrapper/jidctred-8.c src/wrapper/jidctred-12.c src/jmemmgr.c
        src/jmemnobs.c src/wrapper/jquant1-8.c src/wrapper/jquant1-12.c
        src/wrapper/jquant2-8.c src/wrapper/jquant2-12.c src/wrapper/jutils-8.c
        src/wrapper/jutils-12.c src/wrapper/jutils-16.c src/jaricom.c
        src/jdarith.c src/jpeg_nbits.c
      )
      list(TRANSFORM GMB_JPEG_DECODER_SOURCES PREPEND "${GMB_JPEG_SOURCE}/")
      add_library(gmb_jpeg_decoder STATIC ${GMB_JPEG_DECODER_SOURCES})
      target_include_directories(gmb_jpeg_decoder SYSTEM PUBLIC "${GMB_JPEG_SOURCE}/src" "${GMB_JPEG_BINARY}")
      target_compile_definitions(gmb_jpeg_decoder PRIVATE _CRT_SECURE_NO_WARNINGS)
      set_property(TARGET gmb_jpeg_decoder PROPERTY MSVC_RUNTIME_LIBRARY MultiThreadedDLL)
    endfunction()
    gmb_add_windows_jpeg()
    set(GMB_ZLIB_SOURCES adler32.c crc32.c inflate.c inffast.c inftrees.c zutil.c)
    list(TRANSFORM GMB_ZLIB_SOURCES PREPEND "${CMAKE_CURRENT_SOURCE_DIR}/third_party/zlib/")
    add_library(gmb_zlib_decoder STATIC ${GMB_ZLIB_SOURCES})
    target_include_directories(gmb_zlib_decoder SYSTEM PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/third_party/zlib")
    target_compile_definitions(gmb_zlib_decoder PRIVATE _CRT_SECURE_NO_WARNINGS)
    set_property(TARGET gmb_zlib_decoder PROPERTY MSVC_RUNTIME_LIBRARY MultiThreadedDLL)
    # Generate a read-only libpng configuration from its unchanged upstream header.
    file(READ "${CMAKE_CURRENT_SOURCE_DIR}/third_party/libpng/scripts/pnglibconf.h.prebuilt" GMB_PNG_CONFIG)
    string(REGEX REPLACE "#define PNG_(WRITE|SIMPLIFIED_WRITE|SAVE_INT_32)[^\n]*\n" "" GMB_PNG_CONFIG "${GMB_PNG_CONFIG}")
    file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/png-config")
    file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/png-config/pnglibconf.h" "/* GeoModelBridge generated decoder-only configuration. */\n${GMB_PNG_CONFIG}")
    set(GMB_PNG_SOURCES png.c pngerror.c pngget.c pngmem.c pngpread.c pngread.c
      pngrio.c pngrtran.c pngrutil.c pngset.c pngtrans.c)
    list(TRANSFORM GMB_PNG_SOURCES PREPEND "${CMAKE_CURRENT_SOURCE_DIR}/third_party/libpng/")
    add_library(gmb_png_decoder STATIC ${GMB_PNG_SOURCES})
    target_include_directories(gmb_png_decoder SYSTEM PUBLIC
      "${CMAKE_CURRENT_SOURCE_DIR}/third_party/libpng" "${CMAKE_CURRENT_BINARY_DIR}/png-config")
    target_compile_definitions(gmb_png_decoder PRIVATE _CRT_SECURE_NO_WARNINGS PNG_INTEL_SSE_OPT=0)
    target_link_libraries(gmb_png_decoder PRIVATE gmb_zlib_decoder)
    set_property(TARGET gmb_png_decoder PROPERTY MSVC_RUNTIME_LIBRARY MultiThreadedDLL)
    target_link_libraries(GeoModelBridge.NativeWriter PRIVATE gmb_png_decoder)
    install(FILES third_party/libpng/LICENSE DESTINATION licenses/libpng)
    install(FILES third_party/zlib/LICENSE DESTINATION licenses/zlib)
    target_link_libraries(GeoModelBridge.NativeWriter PRIVATE gmb_jpeg_decoder)
    install(FILES third_party/libjpeg-turbo/LICENSE.md third_party/libjpeg-turbo/README.ijg
      DESTINATION licenses/libjpeg-turbo)
    target_sources(GeoModelBridge.NativeWriter PRIVATE backends/native-filegdb/platform_windows.cpp "${CMAKE_CURRENT_SOURCE_DIR}/src/platform_files_windows.cpp")
    set_property(TARGET GeoModelBridge.NativeWriter PROPERTY MSVC_RUNTIME_LIBRARY MultiThreadedDLL)
    target_compile_definitions(GeoModelBridge.NativeWriter PRIVATE NOMINMAX UNICODE _UNICODE _ITERATOR_DEBUG_LEVEL=0)
    target_compile_options(GeoModelBridge.NativeWriter PRIVATE /W4 /permissive- /utf-8)
    target_link_libraries(GeoModelBridge.NativeWriter PRIVATE "${FILEGDB_API_ROOT}/lib64/FileGDBAPI.lib")
  else()
    find_package(PNG REQUIRED)
    find_package(JPEG REQUIRED)
    target_sources(GeoModelBridge.NativeWriter PRIVATE backends/native-filegdb/platform_linux.cpp "${CMAKE_CURRENT_SOURCE_DIR}/src/platform_files_linux.cpp")
    target_compile_options(GeoModelBridge.NativeWriter PRIVATE -Wall -Wextra -Wpedantic)
    target_link_libraries(GeoModelBridge.NativeWriter PRIVATE "${FILEGDB_API_ROOT}/lib/libFileGDBAPI.so" PNG::PNG JPEG::JPEG)
    # FileGDBAPI itself depends on fgdbunixrtl. DT_RPATH also resolves that indirect
    # dependency beside the installed writer; never retain a development SDK path.
    target_link_options(GeoModelBridge.NativeWriter PRIVATE "-Wl,--disable-new-dtags")
    set_target_properties(GeoModelBridge.NativeWriter PROPERTIES BUILD_RPATH "${FILEGDB_API_ROOT}/lib" INSTALL_RPATH "$ORIGIN")
  endif()
  set_target_properties(GeoModelBridge.NativeWriter PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY "${GMB_RUNTIME_DIRECTORY}/native-filegdb")
  # A targeted CLI build must also produce the executable it dispatches.
  add_dependencies(geomodelbridge GeoModelBridge.NativeWriter)
  install(TARGETS GeoModelBridge.NativeWriter RUNTIME DESTINATION bin/native-filegdb)
  if(GMB_INSTALL_FILEGDB_RUNTIME)
    if(WIN32)
      set(GMB_FILEGDB_RUNTIME "${FILEGDB_API_ROOT}/bin64/FileGDBAPI.dll")
      install(FILES "${FILEGDB_API_ROOT}/bin64/FileGDBAPI.dll" DESTINATION bin/native-filegdb)
      install(FILES "${FILEGDB_API_ROOT}/license/Apache License.pdf" "${FILEGDB_API_ROOT}/license/userestrictions.txt" "${FILEGDB_API_ROOT}/README-windows_VS2022.txt" DESTINATION licenses/filegdb-api)
      install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/backends/native-filegdb/sdk-sources.json" DESTINATION licenses/filegdb-api)
    else()
      set(GMB_FILEGDB_RUNTIME "${FILEGDB_API_ROOT}/lib/libFileGDBAPI.so" "${FILEGDB_API_ROOT}/lib/libfgdbunixrtl.so")
      # Exercise the copied runtime during build-tree probes too, instead of
      # accidentally succeeding through an absolute development SDK RPATH.
      set_target_properties(GeoModelBridge.NativeWriter PROPERTIES BUILD_WITH_INSTALL_RPATH TRUE)
      install(FILES "${FILEGDB_API_ROOT}/lib/libFileGDBAPI.so" "${FILEGDB_API_ROOT}/lib/libfgdbunixrtl.so" DESTINATION bin/native-filegdb)
      install(DIRECTORY "${FILEGDB_API_ROOT}/license/" DESTINATION licenses/filegdb-api)
      install(FILES "${FILEGDB_API_ROOT}/README" "${CMAKE_CURRENT_SOURCE_DIR}/backends/native-filegdb/sdk-sources-linux.json" DESTINATION licenses/filegdb-api)
    endif()
    foreach(runtime IN LISTS GMB_FILEGDB_RUNTIME)
      if(NOT EXISTS "${runtime}")
        message(FATAL_ERROR "Missing FileGDB runtime: ${runtime}. Fetch the complete official SDK with scripts/fetch_filegdb_sdk.py.")
      endif()
    endforeach()
    # Run even for a targeted, up-to-date writer build, so a removed DLL is
    # restored without relinking. TARGET_FILE_DIR adds no dependency (CMP0112).
    add_custom_target(gmb_filegdb_runtime
      COMMAND ${CMAKE_COMMAND} -E make_directory "$<TARGET_FILE_DIR:GeoModelBridge.NativeWriter>"
      COMMAND ${CMAKE_COMMAND} -E copy_if_different ${GMB_FILEGDB_RUNTIME} "$<TARGET_FILE_DIR:GeoModelBridge.NativeWriter>"
      COMMENT "Copying official FileGDB runtime beside the native writer"
      VERBATIM)
    add_dependencies(GeoModelBridge.NativeWriter gmb_filegdb_runtime)
    message(STATUS "Native FileGDB runtime: bundled beside the built writer and installed to bin/native-filegdb (licenses in licenses/filegdb-api)")
  else()
    message(WARNING "FileGDB runtime bundling is OFF: this build/install is not standalone. Supply SDK bin64 via PATH on Windows or SDK lib via LD_LIBRARY_PATH on Linux. Set GMB_INSTALL_FILEGDB_RUNTIME=ON to bundle it. Existing copied libraries are not removed.")
  endif()
  if(BUILD_TESTING AND GMB_BUILD_TESTS AND Python3_Interpreter_FOUND AND GMB_INSTALL_FILEGDB_RUNTIME)
    add_test(NAME native_runtime COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/native_runtime_test.py" --writer "$<TARGET_FILE:GeoModelBridge.NativeWriter>")
    add_test(NAME native_build_layout COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/tests/native_build_layout_test.py" --cli "$<TARGET_FILE:geomodelbridge>" --writer "$<TARGET_FILE:GeoModelBridge.NativeWriter>")
  endif()
else()
  message(STATUS "Core-only build: inspect/prepare/fixture are available; GDB conversion requires a separately supplied native writer. Use -DGMB_BUILD_NATIVE=ON -DFILEGDB_API_ROOT=<SDK> for a full build.")
endif()
install(DIRECTORY docs DESTINATION . PATTERN "*.lock" EXCLUDE)
install(FILES README.md VERSION CHANGELOG.md THIRD_PARTY_NOTICES.md DESTINATION .)
install(FILES third_party/ufbx/LICENSE DESTINATION licenses/ufbx)
install(FILES third_party/nlohmann/LICENSE.MIT DESTINATION licenses/nlohmann)
